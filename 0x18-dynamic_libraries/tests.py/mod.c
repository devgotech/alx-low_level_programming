#include "main.h"

/**
 * add - Divides two integers and returns
 * the remainder of the result.
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Add of number one and number two.
 */
int mod(int a, int b)
{
	return (a % b);
}
